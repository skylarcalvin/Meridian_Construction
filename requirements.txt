# Local development dependencies
# ------------------------------
# For running the synthetic data generator and any local analysis OUTSIDE Fabric.
# Inside Fabric notebooks, the Spark runtime already provides pandas, numpy, and
# scikit-learn; add Faker there with `%pip install faker` or a workspace Environment.
# This file is for reproducing the project locally (see README, "Reproducing this").

# Core data tooling (used by the generator)
pandas>=2.0
numpy>=1.24
faker>=24.0

# Local ML / analysis (mirrors what the Fabric notebooks use; handy for local runs)
scikit-learn>=1.3
mlflow>=2.11

# Notebook support for local development
jupyter>=1.0
ipykernel>=6.29
